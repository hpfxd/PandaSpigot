From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: uRyanxD <familiarodrigues123ro@gmail.com>
Date: Fri, 19 Jun 2026 17:38:48 -0300
Subject: [PATCH] Alternative Chunk Ticking Selection


diff --git a/src/main/java/com/hpfxd/pandaspigot/config/PandaSpigotWorldConfig.java b/src/main/java/com/hpfxd/pandaspigot/config/PandaSpigotWorldConfig.java
index 360ed37b95fce68941c234615b465090b8ea7308..aa2af045e8b5be60b5fae412c8dd29bd2e288639 100644
--- a/src/main/java/com/hpfxd/pandaspigot/config/PandaSpigotWorldConfig.java
+++ b/src/main/java/com/hpfxd/pandaspigot/config/PandaSpigotWorldConfig.java
@@ -59,4 +59,15 @@ public class PandaSpigotWorldConfig {
     @Comment("This option controls whether or not there is a chance for arrow crits to deal extra damage.\n" +
             "By default, this is true (vanilla behaviour)")
     public boolean randomArrowDamage = true;
+
+    @Comment("When enabled, uses Spigot's alternative chunk ticking selection algorithm.\n" +
+        "This algorithm limits the number of chunks processed each tick and randomly\n" +
+        "selects chunks around players for ticking, improving performance at the cost\n" +
+        "of deviating from vanilla chunk ticking behaviour (for example, plant growth\n" +
+        "rates may differ).\n" +
+        "When disabled, uses PandaSpigot's chunk ticking implementation based on\n" +
+        "the vanilla 1.9 algorithm, which more closely matches vanilla 1.8/1.9\n" +
+        "behaviour while still providing performance optimizations.\n" +
+        "Default: false")
+    public boolean useSpigotChunkTickingSelection = false;
 }
diff --git a/src/main/java/net/minecraft/server/Chunk.java b/src/main/java/net/minecraft/server/Chunk.java
index 3cb8d99311e2fa9a4a1173f6ef9e8dee957b20fa..bdbda3e61c86dc298862fe560942a08fdc57e3a5 100644
--- a/src/main/java/net/minecraft/server/Chunk.java
+++ b/src/main/java/net/minecraft/server/Chunk.java
@@ -44,7 +44,7 @@ public class Chunk {
     // PaperSpigot end
     private boolean done;
     private boolean lit;
-    private boolean p;
+    private boolean p; public boolean hasTicked() { return this.p; } // PandaSpigot - OBFHELPER
     private boolean q;
     private boolean r;
     private long lastSaved;
diff --git a/src/main/java/net/minecraft/server/ChunkCoordIntPair.java b/src/main/java/net/minecraft/server/ChunkCoordIntPair.java
index 652c07975c7b3a3c5e3dc68cb60ca5fc52e1848c..d93670b5fd2ee58a7b1d4ccd2479cc43d5583823 100644
--- a/src/main/java/net/minecraft/server/ChunkCoordIntPair.java
+++ b/src/main/java/net/minecraft/server/ChunkCoordIntPair.java
@@ -33,6 +33,16 @@ public class ChunkCoordIntPair {
         }
     }
 
+    // PandaSpigot start - Alternative Chunk Ticking Selection
+    public double distanceToSqr(Entity entity) {
+        double centerX = this.x * 16 + 8;
+        double centerZ = this.z * 16 + 8;
+        double deltaX = centerX - entity.locX;
+        double deltaZ = centerZ - entity.locZ;
+        return deltaX * deltaX + deltaZ * deltaZ;
+    }
+    // PandaSpigot end
+
     public int a() {
         return (this.x << 4) + 8;
     }
diff --git a/src/main/java/net/minecraft/server/PlayerChunkMap.java b/src/main/java/net/minecraft/server/PlayerChunkMap.java
index 1c9dbe212cfbeb571f1e09f224fb7ed905dd475b..6a489b4514d565b47d930a6ed1abf94cd863cf54 100644
--- a/src/main/java/net/minecraft/server/PlayerChunkMap.java
+++ b/src/main/java/net/minecraft/server/PlayerChunkMap.java
@@ -1,5 +1,9 @@
 package net.minecraft.server;
 
+// PandaSpigot start
+import com.google.common.base.Predicate;
+import com.google.common.collect.AbstractIterator;
+// PandaSpigot end
 import com.google.common.collect.Lists;
 import java.util.ArrayList;
 import java.util.Iterator;
@@ -17,6 +21,7 @@ import java.util.HashMap;
 
 public class PlayerChunkMap {
 
+    private static final Predicate<EntityPlayer> NON_SPECTATOR_PREDICATE = player -> player != null && !player.isSpectator(); // PandaSpigot - Alternative Chunk Ticking Selection
     private static final Logger a = LogManager.getLogger();
     private final WorldServer world;
     private final List<EntityPlayer> managedPlayers = Lists.newArrayList();
@@ -37,6 +42,42 @@ public class PlayerChunkMap {
         return this.world;
     }
 
+    // PandaSpigot start - Alternative Chunk Ticking Selection
+    public Iterator<Chunk> getTickableChunks() {
+        final Iterator<PlayerChunk> iterator = this.f.iterator();
+
+        return new AbstractIterator<Chunk>() {
+            @Override
+            protected Chunk computeNext() {
+                while (iterator.hasNext()) {
+                    PlayerChunk playerChunk = iterator.next();
+                    Chunk chunk = playerChunk.getChunk();
+
+                    if (chunk == null) {
+                        continue;
+                    }
+
+                    if (PlayerChunkMap.this.world.spigotConfig.randomLightUpdates && !chunk.u() && chunk.isDone()) {
+                        return chunk;
+                    }
+
+                    if (!chunk.hasTicked()) {
+                        return chunk;
+                    }
+
+                    if (!playerChunk.hasPlayerMatching(PlayerChunkMap.this.g * 16.0D, NON_SPECTATOR_PREDICATE)) {
+                        continue;
+                    }
+
+                    return chunk;
+                }
+
+                return endOfData();
+            }
+        };
+    }
+    // PandaSpigot end
+
     public void flush() {
         long i = this.world.getTime();
         int j;
@@ -357,6 +398,7 @@ public class PlayerChunkMap {
         private int dirtyCount;
         private int f;
         private long g;
+        public Chunk chunk; // PandaSpigot - Alternative Chunk Ticking Selection
 
         // CraftBukkit start - add fields
         private final HashMap<EntityPlayer, Runnable> players = new HashMap<EntityPlayer, Runnable>();
@@ -364,6 +406,7 @@ public class PlayerChunkMap {
         private Runnable loadedRunnable = new Runnable() {
             public void run() {
                 PlayerChunk.this.loaded = true;
+                PlayerChunk.this.chunk = PlayerChunkMap.this.a().chunkProviderServer.getChunkIfLoaded(PlayerChunk.this.location.x, PlayerChunk.this.location.z); // PandaSpigot - Alternative Chunk Ticking Selection
             }
         };
         // CraftBukkit end
@@ -553,6 +596,23 @@ public class PlayerChunkMap {
             }
 
         }
+
+        // PandaSpigot start - Alternative Chunk Ticking Selection
+        public boolean hasPlayerMatching(double range, Predicate<EntityPlayer> predicate) {
+            for (EntityPlayer player : this.b) {
+                if (predicate.apply(player) && this.location.distanceToSqr(player) < range * range) {
+                    return true;
+                }
+            }
+
+            return false;
+        }
+
+        public Chunk getChunk() {
+            return this.chunk;
+        }
+        // PandaSpigot end
+
     }
 
     // CraftBukkit start - Sorter to load nearby chunks first
diff --git a/src/main/java/net/minecraft/server/World.java b/src/main/java/net/minecraft/server/World.java
index 9b86cdd3b83ae8b0c040dc9d250b54684d0b7510..522c63e7f884a8b2202b111c81df9b54384703d5 100644
--- a/src/main/java/net/minecraft/server/World.java
+++ b/src/main/java/net/minecraft/server/World.java
@@ -2348,7 +2348,7 @@ public abstract class World implements IBlockAccess {
         // Spigot start
         int optimalChunks = spigotConfig.chunksPerTick;
         // Quick conditions to allow us to exist early
-        if ( optimalChunks > 0  ) {
+        if ( this.pandaSpigotConfig.useSpigotChunkTickingSelection && optimalChunks > 0 ) { // PandaSpigot - Alternative Chunk Ticking Selection
         // Keep chunks with growth inside of the optimal chunk range
         int chunksPerPlayer = Math.min( 200, Math.max( 1, (int) ( ( ( optimalChunks - players.size() ) / (double) players.size() ) + 0.5 ) ) );
         int randRange = 3 + chunksPerPlayer / 30;
diff --git a/src/main/java/net/minecraft/server/WorldServer.java b/src/main/java/net/minecraft/server/WorldServer.java
index 864b63f20167491ecef7e51be7d170d8b141d166..1c9e429b4107daf0991fe0c61bd5377cf4c22c95 100644
--- a/src/main/java/net/minecraft/server/WorldServer.java
+++ b/src/main/java/net/minecraft/server/WorldServer.java
@@ -372,7 +372,7 @@ public class WorldServer extends World implements IAsyncTaskHandler {
         }
     }
 
-    protected void h() {
+    protected void h_unused() { // PandaSpigot - Moved down
         super.h();
         if (this.worldData.getType() == WorldType.DEBUG_ALL_BLOCK_STATES) {
             // Spigot start
@@ -513,6 +513,168 @@ public class WorldServer extends World implements IAsyncTaskHandler {
         // Spigot End
     }
 
+    // PandaSpigot start - Alternative Chunk Ticking Selection
+    protected void h() {
+        super.h();
+        if (this.pandaSpigotConfig.useSpigotChunkTickingSelection) {
+            tickSpigotChunkTickSelection();
+        } else {
+            tickPandaChunkTickSelection();
+        }
+    }
+
+    private void tickSpigotChunkTickSelection() {
+        if (this.worldData.getType() == WorldType.DEBUG_ALL_BLOCK_STATES) {
+            gnu.trove.iterator.TLongShortIterator iterator = this.chunkTickList.iterator();
+            while (iterator.hasNext()) {
+                iterator.advance();
+                long chunkCoord = iterator.key();
+                this.getChunkAt(World.keyToX(chunkCoord), World.keyToZ(chunkCoord)).b(false);
+            }
+        } else {
+            for (gnu.trove.iterator.TLongShortIterator iterator = this.chunkTickList.iterator(); iterator.hasNext(); ) {
+                iterator.advance();
+                long chunkCoord = iterator.key();
+                int chunkX = World.keyToX(chunkCoord);
+                int chunkZ = World.keyToZ(chunkCoord);
+                // If unloaded, or in process of being unloaded, drop it
+                if ((!this.chunkProvider.isChunkLoaded(chunkX, chunkZ)) || (this.chunkProviderServer.unloadQueue.contains(chunkX, chunkZ))) {
+                    iterator.remove();
+                    continue;
+                }
+                int k = chunkX * 16;
+                int l = chunkZ * 16;
+
+                this.methodProfiler.a("getChunk");
+                Chunk chunk = this.getChunkAt(chunkX, chunkZ);
+                this.methodProfiler.b();
+
+                this.a(k, l, chunk);
+
+                this.methodProfiler.a("tickChunk");
+                chunk.b(false);
+                this.methodProfiler.c("thunder");
+                tickThunder(k, l);
+                this.methodProfiler.c("iceandsnow");
+                tickIceAndSnow(k, l);
+                this.methodProfiler.c("tickBlocks");
+                tickBlocks(chunk, k, l);
+                this.methodProfiler.b();
+            }
+        }
+
+        if (spigotConfig.clearChunksOnTick) {
+            chunkTickList.clear();
+        }
+    }
+
+    private void tickPandaChunkTickSelection() {
+        if (this.worldData.getType() == WorldType.DEBUG_ALL_BLOCK_STATES) {
+            Iterator<Chunk> chunkIterator = this.manager.getTickableChunks();
+            while (chunkIterator.hasNext()) {
+                chunkIterator.next().b(false);
+            }
+        } else {
+            for (Iterator<Chunk> iterator = this.manager.getTickableChunks(); iterator.hasNext(); ) {
+                this.methodProfiler.a("getChunk");
+                Chunk chunk = iterator.next();
+                this.methodProfiler.b();
+
+                int k = chunk.locX * 16;
+                int l = chunk.locZ * 16;
+
+                this.a(k, l, chunk);
+
+                this.methodProfiler.a("tickChunk");
+                chunk.b(false);
+                this.methodProfiler.c("thunder");
+                tickThunder(k, l);
+                this.methodProfiler.c("iceandsnow");
+                tickIceAndSnow(k, l);
+                this.methodProfiler.c("tickBlocks");
+                tickBlocks(chunk, k, l);
+                this.methodProfiler.b();
+            }
+        }
+    }
+
+    private void tickThunder(int k, int l) {
+        if (!this.paperSpigotConfig.disableThunder && this.random.nextInt(100000) == 0 && this.S() && this.R()) { // PaperSpigot - Disable thunder
+            this.m = this.m * 3 + 1013904223;
+            int i1 = this.m >> 2;
+            BlockPosition blockposition = this.a(new BlockPosition(k + (i1 & 15), 0, l + (i1 >> 8 & 15)));
+            if (this.isRainingAt(blockposition)) {
+                this.strikeLightning(new EntityLightning(this, blockposition.getX(), blockposition.getY(), blockposition.getZ()));
+            }
+        }
+    }
+
+    private void tickIceAndSnow(int k, int l) {
+        if (!this.paperSpigotConfig.disableIceAndSnow && this.random.nextInt(16) == 0) { // PaperSpigot - Disable ice and snow
+            this.m = this.m * 3 + 1013904223;
+            int i1 = this.m >> 2;
+            BlockPosition blockposition = this.q(new BlockPosition(k + (i1 & 15), 0, l + (i1 >> 8 & 15)));
+            BlockPosition blockposition1 = blockposition.down();
+
+            if (this.w(blockposition1)) {
+                // CraftBukkit start
+                BlockState blockState = this.getWorld().getBlockAt(blockposition1.getX(), blockposition1.getY(), blockposition1.getZ()).getState();
+                blockState.setTypeId(Block.getId(Blocks.ICE));
+
+                BlockFormEvent iceBlockForm = new BlockFormEvent(blockState.getBlock(), blockState);
+                this.getServer().getPluginManager().callEvent(iceBlockForm);
+                if (!iceBlockForm.isCancelled()) {
+                    blockState.update(true);
+                }
+                // CraftBukkit end
+            }
+
+            if (this.S() && this.f(blockposition, true)) {
+                // CraftBukkit start
+                BlockState blockState = this.getWorld().getBlockAt(blockposition.getX(), blockposition.getY(), blockposition.getZ()).getState();
+                blockState.setTypeId(Block.getId(Blocks.SNOW_LAYER));
+
+                BlockFormEvent snow = new BlockFormEvent(blockState.getBlock(), blockState);
+                this.getServer().getPluginManager().callEvent(snow);
+                if (!snow.isCancelled()) {
+                    blockState.update(true);
+                }
+                // CraftBukkit end
+            }
+
+            if (this.S() && this.getBiome(blockposition1).e()) {
+                this.getType(blockposition1).getBlock().k(this, blockposition1);
+            }
+        }
+    }
+
+    private void tickBlocks(Chunk chunk, int k, int l) {
+        timings.chunkTicksBlocks.startTiming(); // Spigot
+        int i1 = this.getGameRules().c("randomTickSpeed");
+        if (i1 > 0) {
+            for (ChunkSection chunksection : chunk.getSections()) {
+                if (chunksection != null && chunksection.shouldTick()) {
+                    for (int l1 = 0; l1 < i1; ++l1) {
+                        this.m = this.m * 3 + 1013904223;
+                        int i2 = this.m >> 2;
+                        int j2 = i2 & 15;
+                        int k2 = i2 >> 8 & 15;
+                        int l2 = i2 >> 16 & 15;
+
+                        IBlockData iblockdata = chunksection.getType(j2, l2, k2);
+                        Block block = iblockdata.getBlock();
+
+                        if (block.isTicking()) {
+                            block.a(this, new BlockPosition(j2 + k, l2 + chunksection.getYPosition(), k2 + l), iblockdata, this.random);
+                        }
+                    }
+                }
+            }
+        }
+        timings.chunkTicksBlocks.stopTiming(); // Spigot
+    }
+    // PandaSpigot end
+
     protected BlockPosition a(BlockPosition blockposition) {
         BlockPosition blockposition1 = this.q(blockposition);
         AxisAlignedBB axisalignedbb = (new AxisAlignedBB(blockposition1, new BlockPosition(blockposition1.getX(), this.getHeight(), blockposition1.getZ()))).grow(3.0D, 3.0D, 3.0D);
